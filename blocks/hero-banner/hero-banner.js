/**
 * Decorates the hero-banner block.
 * A solid-blue centered call-to-action banner. Content (heading, subtext, CTA
 * button) is authored as default rich text; all styling is handled in CSS, so
 * no DOM transformation is required.
 * @param {Element} block
 */
// eslint-disable-next-line no-unused-vars
export default function decorate(block) {
  // No-op: hero-banner is styled entirely via CSS.
}
